$/items = [{name: "Oat Milk Latte", size: "Grande, Extra Shot", price: 6.45}, {name: "Chocolate Croissant", size: "Warmed", price: 4.25}]
$/storeName = "Sunrise Coffee"
$/subtotal = 10.7
$/tax = 0.96
$/total = 11.66
root = Card(main_column)
main_column = Column([header, items_list, divider, totals, actions])
header = Row([coffee_icon, store_name], "center")
coffee_icon = Icon("favorite")
store_name = Text($/storeName)
items_list = Column(_template($/items, order_item_template))
order_item_template = Row([item_details, item_price], "start", justify="spaceBetween")
item_details = Column([item_name, item_size])
item_name = Text($name, "body")
item_size = Text($size, "caption")
item_price = Text(formatCurrency(value=$price, currency="USD"), "body")
divider = Divider()
totals = Column([subtotal_row, tax_row, total_row])
subtotal_row = Row([subtotal_label, subtotal_value], justify="spaceBetween")
subtotal_label = Text("Subtotal", "caption")
subtotal_value = Text(formatCurrency(value=$/subtotal, currency="USD"), "body")
tax_row = Row([tax_label, tax_value], justify="spaceBetween")
tax_label = Text("Tax", "caption")
tax_value = Text(formatCurrency(value=$/tax, currency="USD"), "body")
total_row = Row([total_label, total_value], justify="spaceBetween")
total_label = Text("#### Total")
total_value = Text(formatCurrency(value=$/total, currency="USD"))
actions = Row([purchase_btn, add_btn])
purchase_btn_text = Text("Purchase")
purchase_btn = Button(Event("purchase"), purchase_btn_text)
add_btn_text = Text("Add to cart")
add_btn = Button(Event("add_to_cart"), add_btn_text)
